name: Trigger Jenkins Deployment
on:
  push:
    branches: [ main ]
jobs:
  trigger_jenkins:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Jenkins
        uses: appleboy/jenkins-action@master
        with:
          url: "http://ec2-13-213-8-145.ap-southeast-1.compute.amazonaws.com:8080/"
          user: "admin"
          password: "0f484358ee8442db89b73e7738bcdad2"
          job: "badminton-shop-deploy"
