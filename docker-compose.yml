services:
  web:
    build: .
    container_name: web
    ports:
      - "3000:3000"
    environment:
      - MONGODB_URI=${MONGODB_URI}
    volumes:
      - ./app:/app/app
      - ./public/images:/app/public/images
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s  # Thêm thời gian chờ ban đầu

  nginx:
    image: nginx:alpine
    container_name: nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      web:
        condition: service_healthy
    restart: unless-stopped
